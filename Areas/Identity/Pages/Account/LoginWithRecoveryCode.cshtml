@page
@model LoginWithRecoveryCodeModel
@{
    ViewData["Title"] = "Verificación de código de recuperación";
}

<h1 class="text-center mb-4">@ViewData["Title"]</h1>
<hr />
<div class="alert alert-info">
    <i class="fas fa-info-circle me-2"></i>
    Has solicitado iniciar sesión con un código de recuperación. Esta sesión no será recordada hasta que proporciones
    un código de aplicación autenticadora al iniciar sesión o desactives la autenticación en dos pasos (2FA) e inicies
    sesión nuevamente.
</div>
<div class="row justify-content-center">
    <div class="col-md-6 col-lg-4">
        <form method="post" class="border p-4 rounded-3 shadow-sm bg-white">
            <div asp-validation-summary="ModelOnly" class="alert alert-danger" role="alert"></div>
            <div class="form-floating mb-3">
                <input asp-for="Input.RecoveryCode" class="form-control" autocomplete="off"
                    placeholder="Código de recuperación" />
                <label asp-for="Input.RecoveryCode" class="form-label">Código de recuperación</label>
                <span asp-validation-for="Input.RecoveryCode" class="text-danger small"></span>
            </div>
            <button type="submit" class="w-100 btn btn-lg btn-primary">
                <i class="fas fa-sign-in-alt me-2"></i>Iniciar sesión
            </button>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}