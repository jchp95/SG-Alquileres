@page
@model PersonalDataModel
@{
    ViewData["Title"] = "Datos Personales";
    ViewData["ActivePage"] = ManageNavPages.PersonalData;
}

<h3>@ViewData["Title"]</h3>

<div class="row">
    <div class="col-md-6">
        <p>Tu cuenta contiene datos personales que nos has proporcionado. Esta página te permite descargar o eliminar
            esos datos.</p>
        <p>
            <strong>Eliminar estos datos borrará permanentemente tu cuenta, y esto no se puede recuperar.</strong>
        </p>
        <div class="d-flex justify-content-start aling-items-center">
            <form id="download-data" asp-page="DownloadPersonalData" method="post">
                <button class="btn btn-primary me-2" type="submit">Descargar</button>
            </form>
            <form>
                <a id="delete" asp-page="DeletePersonalData" class="btn btn-danger">Eliminar</a>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}